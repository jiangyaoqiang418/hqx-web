# 微恩熵算科技官网前端开发规范

## 1. 项目范围

官网包含 8 个页面模块：

1. 首页
2. 解决方案
3. 产品中心
4. 技术能力
5. 应用场景
6. 关于我们
7. 新闻动态
8. 联系我们

页面路由统一为：

| 页面 | 路由 |
| --- | --- |
| 首页 | `/` |
| 解决方案 | `/solutions` |
| 产品中心 | `/products` |
| 技术能力 | `/technology` |
| 应用场景 | `/scenarios` |
| 关于我们 | `/about` |
| 新闻动态 | `/news` |
| 新闻详情 | `/news/[slug]` |
| 联系我们 | `/contact` |

## 2. 确定技术栈

- 核心框架：Nuxt 3
- 开发语言：Vue 3 + TypeScript
- UI 框架：Nuxt UI
- 样式体系：Tailwind CSS + CSS Variables
- 动画方案：GSAP
- 图标方案：设计稿素材优先，缺失图标统一使用 Iconify
- SEO：Nuxt SSR/预渲染 + `@nuxtjs/seo`
- 图片优化：`@nuxt/image`
- 内容管理：Nuxt Content
- 表单校验：Zod
- 包管理工具：pnpm
- 代码规范：ESLint + Prettier

不引入 Element Plus、Ant Design Vue、Bootstrap 及其他动画框架。

## 3. 渲染与路由规范

- 首页、解决方案、产品中心、技术能力、应用场景、关于我们、联系我们采用预渲染。
- 新闻列表与新闻详情采用 SSR，并配置缓存。
- 页面跳转统一使用 `NuxtLink`。
- 公开 URL 发布后不得随意修改；确需修改时配置 301 重定向。
- 新闻正文必须输出在服务端 HTML 中，不依赖客户端请求后再渲染正文。

## 4. 全局视觉规范

### 4.1 设计定位

- 类型：深色科技型 B2B 企业官网
- 业务气质：气象、能源、卫星遥感、人工智能与数据平台
- 视觉特征：深海军蓝背景、青蓝高亮、半透明信息卡片、科技数据图形
- 主题策略：全站固定深色主题，不在页面中间切换亮色主题

### 4.2 真实颜色变量

以下颜色来自 8 张主设计图及品牌 Logo 的像素取样。品牌色、交互色和场景辅助色必须统一引用变量，不允许页面内临时新增近似色。

```css
:root {
  /* 深色背景 */
  --color-bg-page: #040c1c;
  --color-bg-deep: #040414;
  --color-bg-section: #04142c;
  --color-bg-card: #061a36;
  --color-bg-card-strong: #0a1a3a;

  /* 品牌 Logo */
  --color-brand-blue: #0280f2;
  --color-brand-blue-deep: #056ce6;
  --color-brand-cyan: #03a4f7;
  --color-brand-green: #40c75a;

  /* 官网交互色 */
  --color-accent-cyan: #01ecf4;
  --color-accent-cyan-mid: #00c2d2;
  --color-accent-cyan-deep: #009aaa;
  --color-action-start: #2072f4;
  --color-action-end: #0242ab;

  /* 文字 */
  --color-text-primary: #f3f4f6;
  --color-text-secondary: #b1b8c7;
  --color-text-muted: #8f9bad;

  /* 描边与状态 */
  --color-border: rgb(66 126 190 / 35%);
  --color-border-active: rgb(1 236 244 / 70%);
  --color-error: #ff5c6c;
  --color-warning: #dea51a;
  --color-success: #40c75a;

  /* 应用场景编号辅助色 */
  --color-scene-01: #8cb02b;
  --color-scene-02: #347ad6;
  --color-scene-03: #5f52cd;
  --color-scene-04: #0ca9a6;
  --color-scene-05: #10a79f;
  --color-scene-06: #dea51a;
  --color-scene-07: #de7415;
  --color-scene-08: #da73aa;
  --color-scene-09: #4591da;
  --color-scene-10: #70bb74;
}
```

颜色使用规则：

- 主按钮统一使用 `--color-action-start` 到 `--color-action-end` 的线性渐变。
- 当前导航、链接和焦点状态统一使用 `--color-accent-cyan`。
- Logo 必须保留蓝、青、绿三色，不做单色替换。
- 场景辅助色只用于应用场景编号、图标及对应局部强调，不用于主按钮。
- 正文不得使用低对比度蓝色，文字对比度必须达到 WCAG AA。

### 4.3 字体

- 中文字体：`Microsoft YaHei`、`PingFang SC`、系统无衬线字体。
- 英文与数字沿用同一无衬线字体体系。
- 只使用 400、500、600、700 四个字重。
- 每页只有一个 `h1`，标题层级必须按照 `h1 > h2 > h3` 使用。
- 不得仅通过字号和字重模拟标题层级。

### 4.4 间距与尺寸

- 使用 8px 间距基准。
- 页面内容最大宽度：1440px。
- 桌面端左右安全间距：48px。
- 平板端左右安全间距：32px。
- 手机端左右安全间距：16px。
- 卡片圆角：12px。
- 输入框圆角：8px。
- 标准按钮高度：44px。
- 手机端点击热区不得小于 44px。

## 5. 公共组件规范

以下组件必须统一沉淀，不允许在页面中重复实现：

- `SiteHeader`：桌面导航和移动端抽屉导航
- `SiteFooter`：企业信息、快捷导航、联系方式和备案信息
- `PageHero`：内页标题、说明、背景和数据指标
- `BaseButton`：主按钮、次按钮和文字按钮
- `MetricItem`：数据指标及数字动画
- `BusinessCard`：产品、解决方案及应用场景卡片
- `SectionTitle`：区块标题与说明
- `NewsCard`：新闻封面、分类、标题、摘要和时间
- `ContactForm`：留言表单及校验反馈
- `EmptyState`：空数据状态
- `LoadingSkeleton`：页面与列表加载状态

Nuxt UI 只负责抽屉、弹窗、表单和提示反馈等基础交互。所有公开组件必须覆盖 Nuxt UI 默认视觉，并使用本规范定义的设计变量。

## 6. 响应式与移动端减法

### 6.1 断点

- 手机端：小于 768px
- 平板端：768px 至 1199px
- 桌面端：1200px 至 1919px
- 大屏端：1920px 及以上

### 6.2 桌面端

- 按设计稿保持完整信息密度。
- 主内容设置最大宽度，超宽屏只增加两侧留白。
- 复杂架构图保持完整结构，不随视口无限放大。

### 6.3 平板端

- 5 列卡片调整为 2 至 3 列。
- 技术架构、指标和复杂卡片按业务层级重新分组。
- 保留完整业务内容，以重排为主，不大幅删减。
- 导航空间不足时切换为抽屉菜单。

### 6.4 手机端

手机端必须重新组织信息，禁止将桌面设计图整体缩小：

- 首屏只保留主标题、核心说明和一个主要按钮。
- 减少卫星、粒子、光束等非必要装饰。
- 数据指标优先展示最重要的 3 至 4 项。
- 卡片正文限制行数，详细内容进入详情页或折叠区域。
- 多列模块调整为单列或横向滑动。
- 新闻侧栏移动到新闻列表底部。
- 联系页面优先展示电话、邮箱和留言表单，地图延迟加载。
- 删除重复说明、重复按钮及不影响业务理解的装饰信息。
- 不得删除核心业务、企业资质和主要转化入口。

## 7. 动效规范

### 7.1 页面初始化

用户首次进入当前页面时执行一次初始化动画：

1. 页面标题淡入并向上移动。
2. 说明文字延迟进入。
3. 数据指标依次出现并执行数字递增。
4. 核心卡片按顺序进入。
5. 背景执行轻量缩放或视差。

站内返回已访问页面时不重复完整动画，只执行快速淡入。

### 7.2 滚动与交互

- 区块首次进入视口时执行一次滚动动画。
- 卡片悬停允许轻微上移、边框高亮和图片缩放。
- 按钮必须提供 hover、active、focus 和 disabled 状态。
- 动画只改变 `transform` 和 `opacity`。
- 不使用持续运行的大面积粒子动画。
- 不使用原生滚动事件持续计算动画。
- GSAP 动画必须在组件卸载时销毁。
- 支持 `prefers-reduced-motion`，减少动画时直接显示内容。
- 手机端取消鼠标跟随、强视差和复杂连续动画。

## 8. SEO 规范

每个页面必须配置：

- 唯一 `title`
- 独立 `description`
- canonical 地址
- Open Graph 标题、摘要和图片
- 正确的语义化标题层级
- 图片 `alt`
- 面包屑信息

结构化数据包括：

- 全站：Organization、WebSite
- 内页：BreadcrumbList
- 新闻详情：Article
- 联系页面：Organization 联系信息

全站必须生成：

- `sitemap.xml`
- `robots.txt`
- 404 页面
- Open Graph 分享图片
- 百度及主流搜索引擎验证文件

## 9. 图片与静态资源规范

- 图片统一通过 `NuxtImg` 输出。
- 优先使用 AVIF，并兼容输出 WebP。
- 所有图片声明宽高，避免页面布局抖动。
- 首屏主图预加载，其余图片懒加载。
- 桌面、平板和手机端输出不同尺寸图片。
- 手机端根据内容重新裁切主视觉，不加载完整桌面大图。
- 图标优先使用现有 PNG/SVG 素材。
- 标题、正文和 SEO 关键词不得合并进背景图片。
- 单张首屏图片压缩后原则上不超过 500KB。

## 10. 新闻内容规范

Nuxt Content 中每篇新闻必须包含：

- 标题
- slug
- 摘要
- 分类
- 封面图
- 发布时间
- 更新时间
- 作者或发布部门
- SEO 标题
- SEO 描述
- 正文
- 是否推荐

新闻 slug 使用英文或拼音短语，不使用数据库编号作为公开地址。

## 11. 联系表单规范

表单字段：

- 姓名
- 联系电话
- 公司名称
- 电子邮箱
- 留言内容

交互要求：

- 使用 Zod 统一校验。
- 必填项明确标识。
- 错误信息显示在对应字段附近。
- 提交期间禁用按钮并显示加载状态。
- 提交成功后提供明确反馈并重置表单。
- 防止重复提交、机器人提交和脚本注入。
- 联系电话、邮箱和地址支持直接点击或复制。

## 12. 性能与质量标准

- Lighthouse Performance 不低于 85。
- Lighthouse SEO 不低于 95。
- Lighthouse Accessibility 不低于 90。
- LCP 小于 2.5 秒。
- INP 小于 200 毫秒。
- CLS 小于 0.1。
- 首屏不加载地图、非必要新闻和复杂动画资源。
- GSAP 按需加载。
- 第三方脚本延迟执行。
- 禁止出现横向滚动条、图片拉伸和文字遮挡。

## 13. 测试与验收

必须覆盖：

- 手机、平板、桌面和 2K 大屏。
- Chrome、Edge 和 Safari。
- 首次进入和站内切换动效。
- 减少动画模式。
- 导航、抽屉、筛选、新闻详情和联系表单。
- 404、空数据、加载失败和接口异常。
- sitemap、robots、canonical 和结构化数据。
- 图片懒加载、首屏加载和布局稳定性。
- 键盘操作、焦点状态和文字对比度。

最终以设计还原度、响应式适配、SEO 完整性、动效流畅度和核心性能指标共同作为上线标准。
