# 微恩熵算科技官网项目指令

本文件适用于 `frontend/` 目录及其全部子目录。Codex 在分析、生成或修改本项目前，必须先阅读本文件和同目录下的 `前端开发规范.md`。

## 1. 工作原则

- 全程使用中文说明方案、变更和验证结果。
- 以现有设计图和素材为唯一视觉依据，不自行改变品牌风格或信息架构。
- 开始实现页面前，先检查对应设计目录的主设计图和 `素材/` 目录。
- 坚持最小改动，优先复用公共组件和现有素材，不重复实现相同能力。
- 输出必须是可运行代码，不提交伪代码、占位逻辑或无法访问的素材地址。
- 不修改 `frontend/` 之外的设计源文件；如需使用素材，应复制到项目静态资源目录并保留源文件。
- 不将整张页面设计图作为网页背景或主体内容，页面文字、按钮、卡片和数据必须使用真实 HTML 实现。
- 不擅自修改路由、导航名称、Logo、联系方式、表单字段和 SEO 文案。

## 2. 设计资料映射

设计资料位于项目上一级目录：

### 2.1 设计还原优先级

- 页面最终效果以对应目录中的主设计图为准，`前端开发规范.md` 用于约束技术实现、响应式、SEO 和工程质量。
- 当通用组件默认效果、文字说明或现有实现与设计图冲突时，视觉表现优先服从设计图；不得以 UI 框架默认样式替代设计效果。
- 还原范围包括布局比例、内容层级、颜色、字体、间距、圆角、描边、背景、图片裁切、图标位置和交互状态。
- 桌面端以主设计图作为像素级还原基准；平板端和手机端在保持品牌风格与内容优先级的前提下，按照本文件的响应式规则重排和减法。
- 设计图未展示的状态，例如加载、空数据、错误、焦点和移动端菜单，必须沿用同一视觉语言补全，不得引入另一套风格。

### 2.2 素材目录规则

- 每个页面的素材都存放在与该页面同名的上一级目录中，目录层级与 [首页](../首页/) 一致。
- 页面开发时只从对应页面目录及其 `素材/` 子目录获取背景图、Logo、图标和装饰资源。
- 公共 Logo 或箭头等素材如在多个页面目录重复，选择一份作为项目公共资源，禁止重复复制多份。
- 对应目录已有素材时禁止使用网络图片、临时占位图、AI 生成图或自行绘制的近似图替换。
- 素材缺失、命名不清或无法对应设计位置时，先列出缺失项并请求确认，不得猜测替代。
- 复制进 `frontend/public/images/` 时按页面建立英文子目录，并保留上一级设计素材原文件不变。

| 页面 | 路由 | 设计目录 |
| --- | --- | --- |
| 首页 | `/` | [首页](../首页/) |
| 解决方案 | `/solutions` | [解决方案](../解决方案/) |
| 产品中心 | `/products` | [产品中心](../产品中心/) |
| 技术能力 | `/technology` | [技术能力](../技术能力/) |
| 应用场景 | `/scenarios` | [应用场景](../应用场景/) |
| 关于我们 | `/about` | [关于我们](../关于我们/) |
| 新闻动态 | `/news` | [新闻动态](../新闻动态/) |
| 新闻详情 | `/news/[slug]` | 复用新闻动态视觉规范 |
| 联系我们 | `/contact` | [联系我们](../联系我们/) |

每个页面实现前必须执行以下检查：

1. 查看对应目录中的主设计图。
2. 清点 `素材/` 中可直接复用的背景、Logo、图标和按钮素材。
3. 区分内容图片与 UI 元素，UI 元素优先用组件实现。
4. 确认桌面、平板和手机三种布局策略后再编写页面。

## 3. 固定技术栈

- Nuxt 3
- Vue 3 Composition API
- TypeScript 严格模式
- Nuxt UI
- Tailwind CSS + CSS Variables
- GSAP
- Iconify
- `@nuxtjs/seo`
- `@nuxt/image`
- Nuxt Content
- Zod
- pnpm
- ESLint + Prettier

禁止引入：

- Element Plus
- Ant Design Vue
- Bootstrap
- 第二套 UI 框架
- GSAP 之外的动画框架
- 未经说明的全局状态库
- 仅为单个简单功能增加的大型依赖

新增第三方依赖前必须先检查 `package.json`，确认现有能力无法完成，并在交付说明中写明用途。

## 4. 工程结构

项目初始化后按以下职责组织代码：

```text
frontend/
├─ app.vue
├─ nuxt.config.ts
├─ assets/
│  └─ css/
├─ components/
│  ├─ base/
│  ├─ business/
│  ├─ layout/
│  └─ motion/
├─ composables/
├─ content/
│  └─ news/
├─ layouts/
├─ pages/
├─ public/
│  └─ images/
├─ schemas/
├─ server/
├─ types/
├─ utils/
├─ AGENTS.md
└─ 前端开发规范.md
```

约束：

- 页面只负责组合区块，不在页面文件中堆积可复用组件逻辑。
- 基础组件放入 `components/base/`，业务组件放入 `components/business/`。
- 导航和页脚放入 `components/layout/`。
- GSAP 动效封装放入 `components/motion/` 或 composable。
- 表单 Schema 集中放入 `schemas/`。
- 公共类型放入 `types/`，禁止无理由使用 `any`。
- 静态内容图片使用语义化英文文件名，禁止空格和随机编号。

## 5. Vue 与 TypeScript 规则

- 统一使用 `<script setup lang="ts">`。
- Props、Emits、接口响应、内容模型和表单数据必须定义类型。
- 优先使用 `computed` 表达派生状态，不复制状态。
- 不在模板中编写复杂表达式或重复过滤逻辑。
- 组件对外属性保持最小化，不暴露内部实现细节。
- 浏览器 API、GSAP 和仅客户端逻辑必须放在客户端生命周期中，并在卸载时清理。
- 服务端渲染阶段不得直接访问 `window`、`document`、`localStorage` 或 `sessionStorage`。
- 连续滚动值不得写入 Vue 响应式状态触发高频重渲染。

## 6. UI 与样式规则

- 全站固定深色主题，使用 `前端开发规范.md` 中的真实颜色变量。
- 禁止在组件内散落设计稿近似色；新增颜色必须先进入全局语义变量。
- 主按钮使用 `--color-action-start` 到 `--color-action-end` 的渐变。
- 当前导航、链接、焦点和关键高亮使用 `--color-accent-cyan`。
- 应用场景辅助色只用于对应编号、图标和局部强调。
- Logo 保留原始蓝、青、绿三色，不做滤镜变色。
- 使用 8px 间距基准、1440px 最大内容宽度、12px 卡片圆角和 8px 输入框圆角。
- 禁止用内联样式重复定义可复用视觉规则。
- Nuxt UI 只提供交互基础，必须覆盖其默认颜色、圆角、阴影和字体，使其符合设计图。
- 所有按钮、链接、输入框和可点击卡片必须提供 hover、active、focus-visible、disabled 状态。
- 正文、按钮和表单文字对比度必须达到 WCAG AA。

## 7. 公共组件要求

优先实现并复用以下组件：

- `SiteHeader`
- `SiteFooter`
- `PageHero`
- `BaseButton`
- `MetricItem`
- `BusinessCard`
- `SectionTitle`
- `NewsCard`
- `ContactForm`
- `EmptyState`
- `LoadingSkeleton`

发现两个页面存在相同结构时，应抽取公共组件；仅内容相似但布局不同的模块不得强行合并成复杂万能组件。

## 8. 响应式规则

固定断点：

- 手机：`< 768px`
- 平板：`768px - 1199px`
- 桌面：`1200px - 1919px`
- 大屏：`>= 1920px`

必须遵守：

- 移动端不得通过整体缩放还原桌面设计。
- 桌面多列内容在平板调整为 2 至 3 列，在手机调整为单列或可控横向滑动。
- 手机首屏只保留主标题、核心说明和一个主要 CTA。
- 手机数据指标只展示最重要的 3 至 4 项。
- 手机减少卫星、粒子、光束、视差等装饰效果。
- 手机新闻侧栏放到新闻列表之后。
- 手机联系页先展示联系方式和表单，地图延迟加载。
- 平板以重排为主，保留完整业务内容。
- 超宽屏只增加两侧留白，内容宽度不得无限拉伸。
- 任意断点不得出现横向页面滚动、内容遮挡、文字溢出或小于 44px 的主要触控热区。

## 9. 动效规则

- GSAP 仅用于页面首次进入、滚动触发、数字递增和必要的状态过渡。
- 用户首次进入当前页面时，按标题、说明、指标、核心卡片、背景的顺序执行初始化动画。
- 已访问页面再次进入时只执行快速淡入，不重复完整编排。
- 区块滚动进入动画默认只执行一次。
- 只动画化 `transform` 和 `opacity`，禁止动画化宽高、定位和影响布局的属性。
- 禁止原生 `scroll` 监听器持续计算动画。
- 禁止持续运行的大面积粒子、强光和无业务意义的循环动画。
- 所有 GSAP context、timeline 和 ScrollTrigger 必须在组件卸载时销毁。
- 必须支持 `prefers-reduced-motion`，减少动画模式下直接展示最终状态。
- 手机端关闭鼠标跟随、强视差和复杂连续动画。

## 10. SEO 与语义化规则

- 每页只能有一个 `h1`，标题必须按 `h1 > h2 > h3` 顺序组织。
- 使用 `useSeoMeta` 或项目统一 SEO composable 配置 title、description 和 Open Graph。
- 每页配置 canonical。
- 全站生成 sitemap、robots、404 和分享图。
- 全站加入 Organization、WebSite 结构化数据。
- 内页加入 BreadcrumbList，新闻详情加入 Article。
- 关键文字必须存在于服务端 HTML，不得只存在于图片、Canvas 或客户端异步渲染结果中。
- 新闻列表和详情采用 SSR，并配置缓存；其他公开页面采用预渲染。
- 图片必须提供准确 `alt`，纯装饰图片使用空 `alt`。
- 公开路由发布后不得直接修改；变更时必须配置 301 重定向。

## 11. 图片与资源规则

- 内容图片统一使用 `NuxtImg`。
- 输出 AVIF 和 WebP，并根据断点提供合适尺寸。
- 所有图片必须声明宽高或固定比例，避免 CLS。
- 首屏主图预加载，其余图片懒加载。
- 手机端使用重新裁切和压缩的资源，不直接下载完整桌面大图。
- 单张首屏图片压缩后原则上不超过 500KB。
- 不拉伸、变形或重新着色品牌 Logo。
- 不把标题、按钮、正文或关键数据烘焙进背景图片。

## 12. 内容与表单规则

新闻内容模型至少包含：标题、slug、摘要、分类、封面图、发布时间、更新时间、发布部门、SEO 标题、SEO 描述、正文、推荐状态。

联系表单固定包含：姓名、联系电话、公司名称、电子邮箱、留言内容。

- 使用 Zod 定义表单 Schema。
- 错误信息显示在对应字段附近。
- 提交期间禁用按钮并显示加载状态。
- 提交成功后显示明确反馈并重置表单。
- 必须处理重复提交、接口失败、脚本注入和机器人提交。
- 未提供真实提交接口前，不伪造成功响应；使用明确的开发占位适配层并标注待接入项。

## 13. 性能和无障碍底线

目标：

- Lighthouse Performance `>= 85`
- Lighthouse SEO `>= 95`
- Lighthouse Accessibility `>= 90`
- LCP `< 2.5s`
- INP `< 200ms`
- CLS `< 0.1`

实施要求：

- GSAP、地图和非首屏模块按需加载。
- 首屏不加载地图、非必要新闻数据和复杂动画资源。
- 第三方脚本延迟执行。
- 保留可见的键盘焦点状态。
- 所有交互支持键盘访问。
- 表单字段具有可关联的 label、错误说明和状态提示。
- 不以颜色作为唯一的信息表达方式。

## 14. 变更与验证流程

每次实现或修改必须执行：

1. 检查对应设计图、素材和现有实现。
2. 明确本次影响的页面、组件、断点、SEO 和动效范围。
3. 优先修改已有公共组件，避免页面级重复代码。
4. 运行项目已有的类型检查、Lint、测试和构建脚本。
5. 在手机、平板、桌面和 2K 尺寸检查布局。
6. 检查首次进入、返回页面及减少动画模式。
7. 检查标题层级、元信息、图片 alt 和结构化数据。
8. 交付时说明修改文件、验证结果、遗留接口和已知风险。

如果 `package.json` 尚不存在，应先按本规范规划项目初始化，不得擅自替换技术栈。项目创建后，以 `package.json` 中实际脚本为准执行验证，不得编造命令或声称未运行的检查已经通过。
